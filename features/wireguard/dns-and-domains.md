# DNS and domains

To change / add DNS settings or a DNS search domain:

* Go to Locations
* From action menu select **Edit.**
* In the Internal VPN section you can modify DNS field. Enter the IP addresses of DNS servers (separated by commas ",") and a search domain

For example:

<figure><img src="../../.gitbook/assets/image (313).png" alt=""><figcaption></figcaption></figure>
